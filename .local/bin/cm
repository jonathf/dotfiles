#!/bin/bash

if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
    extra_args="--git-dir=$HOME/.config/dotfiles --work-tree=$HOME"
fi
if [[ -z "$@" ]]; then
    git $extra_args commit --all
else
    git $extra_args commit "$@"
fi
